import { getLogger } from 'log4js';

export const todoLogger = getLogger('todo');
export const userLogger = getLogger('user');
export const authLogger = getLogger('auth');
export const startupLog = getLogger('startup');